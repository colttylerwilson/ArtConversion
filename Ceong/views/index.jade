extends layout

block content
    h1= title
    form#formAddUser(name="Text",method="post",action="/addtext")
        input#inputUserName(type="text", placeholder="text", name="text")
        button#btnSubmit(type="submit") submit